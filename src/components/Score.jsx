import React, { useState } from "react";

export default function Score() {
  const [score, setScore] = useState(null);

  const handleChange = () => {};

  return (
    <div>
      <input type="text" />
    </div>
  );
}
